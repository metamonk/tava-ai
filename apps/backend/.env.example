# Server Configuration
PORT=8080
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# Database (AWS RDS via SSH tunnel)
# 1. Get the SSH key from team lead (~/.ssh/tava-bastion-key.pem)
# 2. Run `pnpm run tunnel` to establish SSH tunnel
# 3. Database will be accessible at localhost:5432
DATABASE_URL=postgresql://tavaadmin:PASSWORD@localhost:5432/tava_ai

# OpenAI API Key (for AI features)
OPENAI_API_KEY=sk-...
